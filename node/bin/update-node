#!/usr/bin/env zsh

# Load
. $DOTFILES/node/.zsh/nodenv.zsh

# Install node modules
pushd $DOTFILES > /dev/null
npm install -g npm
npm install
popd > /dev/null

# Reload NVM
. $DOTFILES/node/.zsh/nodenv.zsh

echo Done updating node

